# Copy to `secrets.toml` (git-ignored) and fill in your key.
# Without it the app runs normally; the AI explanation buttons simply do not appear.

LLM_API_KEY = "your-provider-api-key"
LLM_BASE_URL = "https://api.groq.com/openai/v1"
LLM_MODEL = "openai/gpt-oss-120b"
